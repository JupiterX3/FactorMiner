{
  "factor_id": "wq101_alpha099",
  "name": "WQ101-Alpha099",
  "description": "WorldQuant Alpha#099。原始公式：(rank(correlation(sum((high+low)/2,20), sum(adv60,20), 9)) < rank(correlation(low,volume,6))) ? -1 : 1。 语义折衷：去掉全部 rank；adv60 = ts_mean(volume,60)。",
  "category": "basic_kline",
  "subcategory": "technical",
  "computation_type": "function",
  "computation_data": {
    "function_file": "basic_kline/functions/wq101_alpha099.py",
    "function_code": "def calculate(data, **kwargs):\n    close = data['close']\n    open_ = data['open']\n    high = data['high']\n    low = data['low']\n    volume = data['volume']\n    ret = returns(close)\n    vwap = vwap_proxy(data)\n    return pd.Series(np.where(ts_corr(ts_sum((high + low) / 2, 20), ts_sum(adv(volume, 60), 20), 9) < ts_corr(low, volume, 6), -1.0, 1.0), index=close.index)\n",
    "entry_point": "calculate",
    "imports": [
      "import pandas as pd",
      "import numpy as np",
      "from _alpha_ops import *  # noqa: F401,F403"
    ]
  },
  "parameters": {},
  "dependencies": [],
  "output_type": "series",
  "metadata": {
    "checksum": "3a1d4478",
    "created_at": "2026-04-23T11:18:59.649637",
    "is_window": true,
    "min_warmup_bars": 80,
    "source_family": "wq101"
  }
}