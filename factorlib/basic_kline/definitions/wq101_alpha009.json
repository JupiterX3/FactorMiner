{
  "factor_id": "wq101_alpha009",
  "name": "WQ101-Alpha009",
  "description": "WorldQuant Alpha#009。原始公式：(0<ts_min(delta(close,1),5)) ? delta(close,1) : ((ts_max(delta(close,1),5)<0) ? delta(close,1) : -delta(close,1))。 语义折衷：直接实现。",
  "category": "basic_kline",
  "subcategory": "technical",
  "computation_type": "function",
  "computation_data": {
    "function_file": "basic_kline/functions/wq101_alpha009.py",
    "function_code": "def calculate(data, **kwargs):\n    close = data['close']\n    open_ = data['open']\n    high = data['high']\n    low = data['low']\n    volume = data['volume']\n    ret = returns(close)\n    vwap = vwap_proxy(data)\n    return pd.Series(np.where(ts_min(delta(close, 1), 5) > 0, delta(close, 1), np.where(ts_max(delta(close, 1), 5) < 0, delta(close, 1), -delta(close, 1))), index=close.index)\n",
    "entry_point": "calculate",
    "imports": [
      "import pandas as pd",
      "import numpy as np",
      "from _alpha_ops import *  # noqa: F401,F403"
    ]
  },
  "parameters": {},
  "dependencies": [],
  "output_type": "series",
  "metadata": {
    "checksum": "ca60ae71",
    "created_at": "2026-04-23T11:18:59.629116",
    "is_window": true,
    "min_warmup_bars": 6,
    "source_family": "wq101"
  }
}